# Optional. Defaults to a local SQLite file if unset.
# For production (e.g. Railway managed Postgres), set to something like:
# postgresql://user:password@host:5432/dbname
DATABASE_URL=sqlite:///./patients.db
